import 'package:flutter/material.dart';

abstract final class AppColors {
  // King Auto brand
  static const primary = Color(0xFF205080);
  static const accent = Color(0xFFF0E000);

  // Application surfaces
  static const background = Color(0xFFF1F4F7);
  static const surface = Color(0xFFFFFFFF);

  // Typography
  static const textPrimary = Color(0xFF102A43);
  static const textSecondary = Color(0xFF66788A);

  // UI
  static const border = Color(0xFFD9E2EC);

  // Semantic
  static const success = Color(0xFF1F8A4C);
  static const warning = Color(0xFFD99000);
  static const error = Color(0xFFC62828);
}
